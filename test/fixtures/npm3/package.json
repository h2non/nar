{
  "name": "test",
  "version": "0.1.0",
  "scripts": {
    "prestart": "echo 'prestart ${NODE_NAR}'",
    "start": "node .",
    "stop": "rm -rf .tmp"
  },
  "archive": {
    "dependencies": true
  },
  "dependencies": {
    "foo": "~0.1.0",
    "bar": "~0.1.0"
  }
}
