{
  "name": "test",
  "version": "1.0.0",
  "main": "a/b/sample.js",
  "scripts": {
    "prestart": "echo 'prestart ${NODE_NAR}'",
    "start": "node a/b/sample.js",
    "stop": "rm -rf .tmp"
  },
  "archive": {
    "devDependencies": true,
    "peerDependencies": false
  },
  "dependencies": {
    "some": "~1.0.0"
  }
}
