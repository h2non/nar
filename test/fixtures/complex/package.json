{
  "name": "test",
  "version": "0.1.0",
  "main": "./main",
  "scripts": {
    "prestart": "echo 'prestart ${NODE_NAR}'",
    "start": "node main",
    "stop": "rm -rf .tmp"
  },
  "archive": {
    "dependencies": true,
    "devDependencies": true,
    "binary": true
  },
  "dependencies": {
    "hu": "~0.1.0"
  },
  "devDependencies": {
    "dev": "~0.1.0"
  }
}
